<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hi.dhl.demo.binding">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Binding">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".databind.DatBindActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".viewbind.ViewBindActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".databind.list.DataBindRecycleActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.hi.dhl.demo.binding.navigation.NavigationActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>